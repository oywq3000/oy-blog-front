export interface UserInfo {
  id: string;
  username: string;
  email: string;
  status: number;
  bio?: string;
  avatarUrl: string;
  blogRole: string;
  emailVerified: boolean;
  ipAddress: string;
  lastLogin: string;
  createdAt: string;
}
